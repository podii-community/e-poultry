import 'package:flutter/material.dart';

class FarmLandingPage extends StatelessWidget {
  const FarmLandingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
